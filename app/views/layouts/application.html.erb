<!DOCTYPE html>
<html>
  <head>
    <title>Booker2</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <script src="https://kit.fontawesome.com/41f1aa1c2b.js" crossorigin="anonymous"></script>
    
  
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body class="d-flex flex-column vh-100">
    <header>
        <nav class="navbar navbar-expand-lg navbar-dark  bg-dark">
          <div class="container">
            <a class= navbar-brand href= "/"> Bookers</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown"
                        aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
               <% if user_signed_in? %>
            </button>
          <ul class="navbar-nav ml-auto">
            <li>
              <i class="fa-solid fa-house-chimney"><%= link_to "Home", root_path %></i>
            </li>
            <li>
              <i class="fa-solid fa-users"><%= link_to 'Users', users_path(current_user.id) %></i>
            </li>
            <li>
              <i class="fa-solid fa-book-open"><%= link_to 'Books', books_path %></i>
            </li>
            <li>
              <i class="fa-solid fa-right-from-bracket"><%= link_to "Log out", destroy_user_session_path, method: :delete %></i>
            </li>
          <% else %>
            <li>
              <i class="fa-solid fa-house-chimney"><%= link_to "Home", root_path %></i>
            </li>
            <li>
             <i class="fa-solid fa-link"><%= link_to "About", about_path %></i>
            </li>
            <li>
              <i class="fa-solid fa-user-plus"><%= link_to "Sign up", new_user_registration_path %></i>
            </li>
            <li>
              <i class="fa-solid fa-right-to-bracket"><%= link_to "Log in", new_user_session_path %></i>
            </li>
          </ul>  
          <% end %>
        </div>
      </nav>
    </header>
    <div class="body">
    <p class="notice"><%= notice %></p>
    
    <%= yield %>
    <footer>
      <p class="text-center">CopyRight Infratop.inc</p>
    </footer>
    </div>
  </body>
</html>
